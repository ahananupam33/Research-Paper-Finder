name: Research Paper Finder

on:
  push:
    branches: [main]

jobs:
  # BACKEND JOB
  backend-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install and setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13.9"
          cache: "pip"

      - name: Install Backend dependencies
        run: |
          cd backend
          pip install -r requirements.txt

      - name: Deploy Backend
        if: success()
        run: curl ${{ secrets.RENDER_DEPLOY_HOOK }}

  # FRONTEND JOB
  frontend-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup and install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: paper-search-ui/package-lock.json

      - name: Install Frontend dependencies
        run: |
          cd paper-search-ui
          npm install

      # - name: Build & Lint
      #   run: |
      #     cd paper-search-ui
      #     npm run build

      - name: Build and Deploy frontend to Vercel
        run: |
          npm install --global vercel@latest
          vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
          vercel build  --prod --token=${{ secrets.VERCEL_TOKEN }}
          vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}
        working-directory: ./paper-search-ui
        env:
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
